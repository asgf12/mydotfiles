alias conf="sudo nvim /etc/nixos/configuration.nix"
alias dup="sudo nixos-rebuild switch --upgrade"
alias hyp="nvim .config/hypr/hyprland.conf"
alias cleanup="nix-collect-garbage -d --delete-older-than 20d"

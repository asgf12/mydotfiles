[[ -x $(command -v "flatpak") ]] && alias dup="sudo flatpak update && sudo nixos-rebuild switch --upgrade" || alias dup="sudo nixos-rebuild switch --upgrade"
alias conf="sudo nvim /etc/nixos/configuration.nix"
alias hyp="nvim .config/hypr/hyprland.conf"
alias cleanup="nix-collect-garbage -d --delete-older-than 15d"
